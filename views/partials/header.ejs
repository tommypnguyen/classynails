<!DOCTYPE html>
<html>
    <head>
        <title>Classy Nails</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
        <!-- Make site responsive on mobile/tablet -->
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
        <div class="container">
            <nav class="navbar navbar-default">
                <div class="container">
                    <div class="navbar-header">
                        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                          <span class="sr-only">Toggle navigation</span>
                          <span class="icon-bar"></span>
                          <span class="icon-bar"></span>
                          <span class="icon-bar"></span>
                        </button>
            
                        <a class="navbar-brand" href="/">Classy Nails</a>
                    </div>
                    <div id="navbar" class="collapse navbar-collapse">
                      <ul class="nav navbar-nav">
                        <li class="<%= typeof page !== 'undefined' && page === 'home' ? 'active' : '' %>"><a href="/">Home</a></li>
                        <li class="<%= typeof page !== 'undefined' && page === 'menu' ? 'active' : '' %>"><a href="/">Menu</a></li>
                        <li class="<%= typeof page !== 'undefined' && page === 'work' ? 'active' : '' %>"><a href="/">Our Work</a></li>
                        <li class="<%= typeof page !== 'undefined' && page === 'contact' ? 'active' : '' %>"><a href="contact">Contact Us</a></li>
                      </ul>
                    </div>
                </nav>
  